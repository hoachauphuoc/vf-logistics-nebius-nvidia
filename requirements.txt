# AI model layer - Nebius Token Factory (OpenAI-compatible)
# The openai package's AsyncOpenAI client talks to Token Factory; used by
# nebius_client.py for all four agents (three text, one vision).
openai>=1.40.0

# Google Cloud Services (infrastructure only - no AI SDK from Google is used)
google-cloud-firestore>=2.11.0
google-cloud-pubsub>=2.18.0
google-cloud-storage>=2.10.0

# Web Framework
flask>=2.3.0
gunicorn>=21.2.0
flask-cors>=4.0.0
flask-limiter>=3.5.0

# Utilities
python-dotenv>=1.0.0
pypdf>=4.0.0
httpx>=0.24.0

# pypdfium2 rasterises the first page of an uploaded PDF to PNG before it is sent
# to the vision model - Token Factory's vision models take images, not PDFs.
# pypdfium2 uses Apache 2.0/BSD-3 license (Google's PDFium), no AGPL concerns.
pypdfium2>=4.30.0

# fpdf2 renders event-sourced shipments into a bill of lading so every case has
# paperwork a human can review, not only the ones uploaded as a document.
fpdf2>=2.7.0
